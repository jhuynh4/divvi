spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/divvi
    username: divvi
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  aws:
    s3:
      bucket: divvi-receipts-joey
      region: us-east-1