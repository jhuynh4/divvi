services:
  postgres:
    image: postgres:16
    container_name: divvi-postgres
    environment:
      POSTGRES_DB: divvi
      POSTGRES_USER: divvi
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: